apiVersion: apps/v1
kind: Deployment
metadata:
  name: yourapp-deployment-1
  labels:
    app: yourapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: yourapp
  template:
    metadata:
      labels:
        app: yourapp
    spec:
      containers:
      - name: vijaybabu09/harness_test:MYAPP
        image: nginx:1.21
        ports:
        - containerPort: 80
